ls > out1 >> out1 > out2

echo "unset PATH" | env -i $MINISHELL_PATH"/"$EXECUTABLE = !! no newline

MINI


exit "    1"
dont write exit in case exit 1

export a b= ->segfault

unset HOME PWD OLDPWD
cd -> segfault

unset HOME PWD OLDPWD
cd -  -> segfault

./ -> leaks

export PATH="asdads"
./cat (is in same dir) -> shouldnt work

unset PATH
cat -> leaks

open fds

heredoc ctrl d -> leaks

<< eof cat | << eof cat | sleep 5 -> shouldnt sleep

export a=" a b "
echo $a -> leaks

export a="                                " 
echo $a -> leaks